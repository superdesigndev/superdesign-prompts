# Dropped from the mirror

844 of 964 prompts fell below the top-120 usage cutoff (score = copyCount*2 + tryCount >= 2507).

Full data retained in `data/all-prompts.json`; regenerate with a higher TOP_N anytime.
